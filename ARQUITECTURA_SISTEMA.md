# 🏗️ ARQUITECTURA DEL SISTEMA EDUFINANZAS

## 📐 DIAGRAMA DE ARQUITECTURA

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (React)                         │
│                     http://localhost:5173                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  Dashboard   │  │   Profile    │  │ TemaDetalle  │          │
│  │   (User)     │  │    (User)    │  │    (User)    │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                  │                  │                   │
│         └──────────────────┼──────────────────┘                  │
│                            │                                      │
│                   ┌────────▼─────────┐                           │
│                   │  UserAuthContext │                           │
│                   │   (JWT Token)    │                           │
│                   └────────┬─────────┘                           │
│                            │                                      │
│                   ┌────────▼─────────┐                           │
│                   │   API Services   │                           │
│                   │   (Axios HTTP)   │                           │
│                   └────────┬─────────┘                           │
└────────────────────────────┼─────────────────────────────────────┘
                             │
                    HTTP/HTTPS (JSON)
                             │
┌────────────────────────────▼─────────────────────────────────────┐
│                    BACKEND (Django REST)                          │
│                   http://localhost:8000/api                       │
├───────────────────────────────────────────────────────────────────┤
│                                                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                     ENDPOINTS (Views)                     │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │  • GET  /api/perfil/me/               (PerfilMeView)     │   │
│  │  • PUT  /api/perfil/me/update/        (PerfilMeUpdate)   │   │
│  │  • GET  /api/perfil/me/progreso/      (ProgresoMeView)   │   │
│  │  • GET  /api/temas/{id}/retos/        (RetosPorTema)     │   │
│  │  • POST /api/retos/{id}/iniciar/      (IniciarReto)      │   │
│  └────────────────────┬─────────────────────────────────────┘   │
│                       │                                           │
│  ┌────────────────────▼─────────────────────────────────────┐   │
│  │              JWT TOKEN EXTRACTION                         │   │
│  │  extraer_usuario_de_token(request)                        │   │
│  │  obtener_perfil_de_usuario(id_usuario)                    │   │
│  └────────────────────┬─────────────────────────────────────┘   │
│                       │                                           │
│  ┌────────────────────▼─────────────────────────────────────┐   │
│  │                 SERVICE LAYER                             │   │
│  │  • iniciar_reto_service()                                 │   │
│  │  • calcular_progreso_usuario_service()                    │   │
│  │  • obtener_retos_por_tema_service()                       │   │
│  └────────────────────┬─────────────────────────────────────┘   │
│                       │                                           │
└───────────────────────┼───────────────────────────────────────────┘
                        │
              SQL CALLS (PyMySQL)
                        │
┌───────────────────────▼───────────────────────────────────────────┐
│                   DATABASE (MySQL 8.0)                             │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐│
│  │              STORED PROCEDURES (Business Logic)              ││
│  ├──────────────────────────────────────────────────────────────┤│
│  │  1. obtener_perfil_por_usuario(id_usuario)                   ││
│  │  2. iniciar_reto(id_perfil, id_reto)                         ││
│  │  3. obtener_retos_por_tema(id_tema, id_perfil)               ││
│  │  4. solucionar_reto(id_perfil, id_reto, respuesta)           ││
│  │  5. calcular_progreso_usuario(id_perfil)                     ││
│  └──────────────────────┬───────────────────────────────────────┘│
│                         │                                          │
│  ┌──────────────────────▼───────────────────────────────────────┐│
│  │                    DATA TABLES                                ││
│  ├──────────────────────────────────────────────────────────────┤│
│  │  • usuario (id_usuario, correo, rol, contrasena)             ││
│  │  • perfil (id_perfil, id_usuario, nombre_perfil, monedas)    ││
│  │  • tema (id_tema, nombre, descripcion, img_tema)             ││
│  │  • reto (id_reto, titulo, costo_moneda, recompensa_moneda)   ││
│  │  • progreso (id_progreso, id_perfil, id_reto, completado)    ││
│  │  • recompensa (id_recompensa, tipo, nombre, descripcion)     ││
│  └──────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 FLUJO DE DATOS: INICIAR RETO

```
┌──────────────┐
│   Usuario    │
│ (Frontend)   │
└──────┬───────┘
       │
       │ 1. Click "Iniciar Reto"
       │
       ▼
┌──────────────────────────┐
│  TemaDetalle.jsx         │
│  handleIniciarReto()     │
└──────┬───────────────────┘
       │
       │ 2. Validar monedas localmente
       │
       ▼
┌──────────────────────────┐
│  api.js                  │
│  iniciarReto(idReto)     │
└──────┬───────────────────┘
       │
       │ 3. POST /api/retos/{id}/iniciar/
       │    Headers: { Authorization: "Bearer {token}" }
       │
       ▼
┌──────────────────────────────────┐
│  IniciarRetoView (Backend)       │
│  1. Extraer id_usuario del token │
│  2. Obtener perfil asociado      │
└──────┬───────────────────────────┘
       │
       │ 4. Llamar servicio
       │
       ▼
┌──────────────────────────────────┐
│  iniciar_reto_service()          │
│  (progresos/services.py)         │
└──────┬───────────────────────────┘
       │
       │ 5. CALL iniciar_reto(id_perfil, id_reto)
       │
       ▼
┌──────────────────────────────────────────┐
│  Stored Procedure: iniciar_reto          │
│  1. Validar monedas suficientes          │
│  2. UPDATE perfil SET monedas = monedas - costo │
│  3. INSERT INTO progreso (...)           │
└──────┬───────────────────────────────────┘
       │
       │ 6. RETURN id_progreso
       │
       ▼
┌──────────────────────────────────┐
│  IniciarRetoView                 │
│  Retornar JSON con progreso      │
└──────┬───────────────────────────┘
       │
       │ 7. Response: { message, progreso, perfil }
       │
       ▼
┌──────────────────────────────────┐
│  TemaDetalle.jsx                 │
│  1. recargarPerfil()             │
│  2. Actualizar lista de retos    │
│  3. Mostrar mensaje de éxito     │
└──────┬───────────────────────────┘
       │
       │ 8. UI actualizada
       │
       ▼
┌──────────────┐
│   Usuario    │
│ Ve monedas   │
│ actualizadas │
└──────────────┘
```

---

## 🔐 AUTENTICACIÓN Y AUTORIZACIÓN

### Flujo de Autenticación JWT

```
┌──────────────┐
│   Usuario    │
│  Hace Login  │
└──────┬───────┘
       │
       │ POST /api/login_usuario/
       │ { correo, contrasena }
       │
       ▼
┌──────────────────────────┐
│  LoginView (Backend)     │
│  1. Validar credenciales │
│  2. Generar JWT token    │
└──────┬───────────────────┘
       │
       │ Response: { token, usuario }
       │
       ▼
┌──────────────────────────┐
│  UserAuthContext         │
│  1. Guardar token en     │
│     localStorage         │
│  2. Cargar perfil desde  │
│     /api/perfil/me/      │
└──────┬───────────────────┘
       │
       │ Estado global actualizado
       │
       ▼
┌──────────────────────────┐
│  Todas las peticiones    │
│  subsecuentes incluyen:  │
│  Authorization: Bearer   │
│  {token}                 │
└──────────────────────────┘
```

### Estructura del JWT Token

```json
{
  "header": {
    "alg": "HS256",
    "typ": "JWT"
  },
  "payload": {
    "id_usuario": 1,
    "correo": "user@example.com",
    "rol": "Usuario",
    "exp": 1700000000
  },
  "signature": "..."
}
```

---

## 📊 MODELO DE DATOS

### Relaciones entre Tablas

```
┌────────────────┐
│    usuario     │
│ ────────────── │
│ id_usuario (PK)│
│ correo         │
│ rol            │
│ contrasena     │
└────────┬───────┘
         │ 1
         │
         │ 1:1
         │
         ▼
┌────────────────┐
│    perfil      │
│ ────────────── │
│ id_perfil (PK) │
│ id_usuario (FK)│
│ nombre_perfil  │
│ edad           │
│ monedas        │◄──────────────┐
└────────┬───────┘               │
         │ 1                     │
         │                       │
         │ 1:N                   │
         │                       │
         ▼                       │
┌────────────────┐               │
│   progreso     │               │
│ ────────────── │               │
│ id_progreso(PK)│               │
│ id_perfil (FK) │───────────────┘
│ id_reto (FK)   │───────────┐
│ completado     │           │
└────────────────┘           │
                             │
                             │ N:1
                             │
                             ▼
                    ┌────────────────┐
                    │      reto      │
                    │ ────────────── │
                    │ id_reto (PK)   │
                    │ id_tema (FK)   │───┐
                    │ titulo         │   │
                    │ costo_moneda   │   │
                    │ recompensa_    │   │
                    │   moneda       │   │
                    └────────────────┘   │
                                         │ N:1
                                         │
                                         ▼
                                ┌────────────────┐
                                │      tema      │
                                │ ────────────── │
                                │ id_tema (PK)   │
                                │ nombre         │
                                │ descripcion    │
                                │ img_tema       │
                                └────────────────┘
```

---

## 🎯 COMPONENTES FRONTEND

### Jerarquía de Componentes

```
App.jsx
│
├── AuthContext (Admin)
│   └── token (localStorage)
│
├── UserAuthContext (Usuario)
│   ├── userToken (localStorage)
│   ├── usuario
│   └── perfil
│
└── AppRouter
    │
    ├── AdminRoute (protección admin)
    │   └── AdminLayout
    │       ├── Header
    │       ├── Sidebar
    │       ├── [Página Admin]
    │       └── Footer
    │
    └── UserRoute (protección usuario)
        └── UserLayout
            ├── UserHeader
            │   ├── Logo
            │   ├── Monedas (perfil.monedas)
            │   ├── Botón "Mi Perfil"
            │   ├── Info Usuario
            │   └── Botón "Cerrar Sesión"
            │
            └── [Página Usuario]
                │
                ├── Dashboard
                │   ├── Tip del Día (rotación)
                │   ├── Barra de Progreso
                │   └── Grid de Temas
                │
                ├── Profile
                │   ├── Avatar
                │   ├── Información Personal
                │   ├── Form Edición
                │   └── Cambiar Contraseña
                │
                └── TemaDetalle
                    ├── Header del Tema
                    ├── Grid de Retos
                    │   └── RetoCard
                    │       ├── Badge Estado
                    │       ├── Metadata (costo, recompensa)
                    │       └── Botón Acción
                    └── Mensaje Feedback
```

---

## 🔧 TECNOLOGÍAS UTILIZADAS

### Backend Stack
```
┌─────────────────────────────┐
│ Django 5.2                  │  Framework web
├─────────────────────────────┤
│ Django REST Framework       │  API REST
├─────────────────────────────┤
│ PyJWT                       │  Autenticación JWT
├─────────────────────────────┤
│ MySQLClient                 │  Conector MySQL
├─────────────────────────────┤
│ Pillow                      │  Manejo de imágenes
└─────────────────────────────┘
```

### Frontend Stack
```
┌─────────────────────────────┐
│ React 18                    │  UI Library
├─────────────────────────────┤
│ React Router DOM v6         │  Routing
├─────────────────────────────┤
│ Axios                       │  HTTP Client
├─────────────────────────────┤
│ Context API                 │  State Management
├─────────────────────────────┤
│ Vite                        │  Build Tool
└─────────────────────────────┘
```

### Database
```
┌─────────────────────────────┐
│ MySQL 8.0                   │  Base de datos
├─────────────────────────────┤
│ Stored Procedures           │  Lógica de negocio
├─────────────────────────────┤
│ Transacciones ACID          │  Integridad de datos
└─────────────────────────────┘
```

---

## 📈 ESCALABILIDAD Y MEJORAS FUTURAS

### Posibles Mejoras

1. **Caché de Datos**
   - Redis para cachear tips y temas
   - Reducir carga en BD

2. **WebSockets**
   - Notificaciones en tiempo real
   - Actualización de monedas sin recargar

3. **CDN para Imágenes**
   - Amazon S3 / Cloudinary
   - Optimización de carga

4. **Microservicios**
   - Separar autenticación
   - Servicio de gamificación
   - Servicio de analytics

5. **Testing**
   - Jest + React Testing Library
   - PyTest para backend
   - E2E con Cypress

---

## 🔒 SEGURIDAD

### Medidas Implementadas

- ✅ Contraseñas hasheadas con bcrypt
- ✅ JWT con expiración de 24 horas
- ✅ Validación de entrada en backend
- ✅ CORS configurado
- ✅ SQL Injection prevenido (stored procedures)
- ✅ XSS prevenido (React escapa HTML)
- ✅ CSRF token en formularios

---

*Documento generado el 2025-11-20*
